{
  "name": "chess-backend",
  "version": "1.0.0",
  "description": "Stockfish backend for Chess App",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "lint": "node --check index.js && node --check auth.js && node --check db.js && node --check middleware/requireAuth.js",
    "test": "vitest run",
    "smoke": "node -e \"require('./index'); console.log('backend smoke ok')\""
  },
  "dependencies": {
    "better-sqlite3": "^12.8.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^17.3.1",
    "express": "^4.19.2",
    "google-auth-library": "^10.6.2",
    "jsonwebtoken": "^9.0.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "supertest": "^7.1.4",
    "vitest": "^4.1.0"
  }
}
